-- Day 2 

SELECT * FROM books;

SELECT * FROM bookstemp;

insert into bookstemp
values
(1,'Martin Luther', 'MJ', 'Biography', 1990);

-- Adding new column in table ('price')

ALTER TABLE bookstemp
ADD COLUMN price DECIMAL(5,2);


UPDATE bookstemp
SET price = 19.97
WHERE bookid = 1;


ALTER TABLE bookstemp
RENAME COLUMN autohor TO Author;


UPDATE bookstemp
SET price = 20.89
WHERE PublishedYear < 1900;


-- update the existing books with prices
UPDATE bookstemp SET price = 12.88 WHERE BookID = 1;
UPDATE bookstemp SET price = 14.00 WHERE BookID = 2;
UPDATE bookstemp SET price = 29.99 WHERE BookID = 3;
UPDATE bookstemp SET price = 20.89 WHERE BookID = 4;
UPDATE bookstemp SET price = 17.37 WHERE BookID = 5;


-- RETRIVE THE DATA FROM TABLES
SELECT * FROM bookstemp;


-- specific columns
SELECT  title, price
FROM bookstemp; 

-- TYPES OF KEYS IN DATABASE
-- 1. PRIMARY KEY: A primary key used to uniquely identify the each record of 
-- table, 
-- Properties : it must contain the unique values


-- trying to insert duplicate data 

INSERT INTO bookstemp 
VALUES 
(1,'Martin','Ronald', 'Sci-Fi', 2002, 34.99);


SELECT * FROM bookstemp;


-- 2 FOREIGN KEY : A Foreign Key is a fielt (a collection of fields) in one 
-- table that refers to the primary key in another table.
-- it establishes a relationship between two tables.

-- 3) UNIQUE KEY : a unique key ensures that all the values in a column are 
-- different, but allows NULL values.

-- 4) COMPOSITE KEY: Composite key combines tow or more columns to form a 
-- unique identifies for a record.


SHOW TABLEs;

DROP TABLE IF EXISTS bookstemp;
